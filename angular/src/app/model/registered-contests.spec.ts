import { RegisteredContests } from './registered-contests';

describe('RegisteredContests', () => {
  it('should create an instance', () => {
    expect(new RegisteredContests()).toBeTruthy();
  });
});
